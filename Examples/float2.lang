func min(x: float, y: float) -> float {
	if (x < y) {
		return x;
	} else {
		return y;
	}
}

func main() -> int {
	let a: float = 4.25f;
	let b: float = 2.74f;
	
	let m: float = min(a, b);

	return cast(int) (m + 0.5f);
}

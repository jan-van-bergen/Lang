func abc(a: int, b: int) -> int {
	let ptr_a: int * = &a;
	let ptr_b: int * = &b;

	let ptr_ptr: int ** = &ptr_a;
	
	*ptr_ptr = ptr_b;

	return **ptr_ptr;
}

func main() -> int {
	return abc(1, 2);
}
